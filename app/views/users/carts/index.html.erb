カートの中身一覧

<table border="1">
	
</style>
	<thead>
		<tr>
			<th>商品名</th>
			<th>個数</th>
			<th>価格</th>
		</tr>
	</thead>
	<tbody>
<% @carts.each do |cart| %>
		<tr>
			<td><%= cart.item.name %></td>
			<td><%= cart.count %></td>
			<td><%= cart.item.price %></td>
			<td><%= link_to "商品を削除", users_cart_path(cart), method: :delete %></td>
		</tr>
	</tbody>
<% end -%>
</table>

小計<br>

<%= link_to "レジに進む",  users_carts_buy_path %>